# Dépendances d'exécution du moteur IA.
# Bornes volontairement souples : après le premier build, `pip freeze` dans le
# conteneur donne le jeu exact à épingler pour la prod.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Dépôt de CV : lecture du fichier envoyé (multipart) et extraction du texte.
python-multipart>=0.0.9
pypdf>=5.0
python-docx>=1.1

# SDK officiel Anthropic. Une version récente est nécessaire : le moteur
# utilise `output_config` (sorties structurées + effort).
anthropic>=0.49
